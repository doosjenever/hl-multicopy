# Alice — crypto trader
# Vul target_wallet in met een echte wallet address

leader:
  name: "alice"
  target_wallet: ""  # TODO: vul in
  sub_account: ""
  starting_equity: 200.0
  paper_mode: true
  slippage_cap_pct: 0.5
  debounce_ms: 80
  reconciler_interval_s: 30
  max_drawdown_pct: 25.0
  daily_loss_limit_pct: 10.0

global:
  log_level: "INFO"
